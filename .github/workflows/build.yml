name: Build ZMK firmware
on:
  workflow_dispatch:
  push:
    paths-ignore:
      - "keymap-drawer/**"

jobs:
  build:
    # Keep in sync with the zmk revision in config/west.yml
    uses: zmkfirmware/zmk/.github/workflows/build-user-config.yml@v0.3.0

